{{- $manifest := dict "name" .Site.Title "short_name" .Site.Title "start_url" "/" "display" "standalone" "orientation" "portrait" "theme_color" "#ffffff" "background_color" "#ffffff" -}}
{{- if .Site.Params.manifest -}}
  {{- $manifest = merge $manifest .Site.Params.manifest -}}
{{- end -}}
{{- $manifest | jsonify -}}